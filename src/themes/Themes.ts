export default abstract class Themes {
	static readonly lightMode: string = 'lightMode';
	static readonly darkMode: string = 'darkMode';
	static readonly allThemes: string[] = [
		Themes.lightMode,
		Themes.darkMode,
	];
}
